<UserControl
    x:Class="EdutalkTeacherUWP.Common.Controls.ItemRouteControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EdutalkTeacherUWP.Common.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="48"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Border CornerRadius="12" Background="{x:Bind ColorBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <local:CircleImageControl Image="{x:Bind Source}" HorizontalAlignment="Center" VerticalAlignment="Center"   Height="32" Width="32"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                <TextBlock Text="{Binding Attendance}" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
